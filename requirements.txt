fastapi
uvicorn[standard]
pydantic
python-multipart
numpy
scikit-learn
lightgbm
optuna
catboost
xgboost
# Local embedding backend (nomic-ai/nomic-embed-text-v1.5). The model weights
# are downloaded once from Hugging Face and then reused from its local cache.
sentence-transformers==5.5.1
# Required by Nomic's custom model implementation.
einops==0.8.2
torch==2.12.0
pytest
httpx
# Optional Redis backend for the /anonymize per-session consistency vault
# (original->fake mappings); selected with vault_backend="redis". The default
# in-memory backend needs neither. fakeredis provides an in-process Redis so the
# Redis-backend tests run without a server.
redis
fakeredis
# Pinned: PII detection/replacement behavior depends on these exact versions, so
# an unplanned upgrade could silently change what leaks or gets over-redacted.
presidio-analyzer==2.2.359
presidio-anonymizer==2.2.362
faker==40.21.0
spacy==3.8.13
# spaCy English NER model for Presidio, pinned so `pip install -r requirements.txt`
# pulls it with no separate `spacy download` step. The model version (3.8.x) must
# stay compatible with the pinned spaCy minor version above; bump both together.
en_core_web_lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl
